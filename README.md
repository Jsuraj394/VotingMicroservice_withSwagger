Both Microservices are communicating with each other so start both of them.
also swagger is there , but now swagger is not fully configure , Some API's should not visible but they are. so without checking the API do not call them.

This project or code is for education purpose only , do not include this in industrial level.
